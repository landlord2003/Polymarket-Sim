# 复制本文件为 .env 并填入你自己的凭据（.env 已被 gitignore，不会上传）
# 不填也能跑：缺 TUSHARE_TOKEN 时，财务/资金流自动降级回东财（再失败用本地代理/回填）

# Tushare token（可选，注册地址 https://tushare.pro ）
# 财务(fina_indicator) 免费档即可；资金流(moneyflow) 需约 200 积分（签到/分享/购买）
TUSHARE_TOKEN=

# 钉钉机器人（run_daily 推送用，已在 notify.py 的 _load_dotenv 加载）
# DINGTALK_WEBHOOK=
# DINGTALK_SECRET=
# WECOM_WEBHOOK=
