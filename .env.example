# 复制本文件为 .env 并填入你自己的凭据（.env 已被 gitignore，不会上传）
# 不填也能跑：缺 TUSHARE_TOKEN 时，财务/资金流自动降级回东财（再失败用本地代理/回填）

# Tushare token（可选，注册地址 https://tushare.pro ）
# 财务(fina_indicator) 免费档即可；资金流(moneyflow) 需约 200 积分（签到/分享/购买）
TUSHARE_TOKEN=

# 钉钉机器人（run_daily 推送用，已在 notify.py 的 _load_dotenv 加载）
# DINGTALK_WEBHOOK=
# DINGTALK_SECRET=
# WECOM_WEBHOOK=

# ============ Polymarket 模拟盘（a_share/sim_server.py） ============
# 关停端点鉴权：/api/shutdown 必须带此 token（query ?token= 或 Authorization: Bearer）
# 不设在局域网等同裸奔（任意主机可关停服务），强烈建议填入随机长串
# SHUTDOWN_TOKEN=

# 成交率影子标定应用开关：1=把测量出的 recommended_base 应用到 FILL_BASE（默认 0=仅测量不改）
# FILL_CALIBRATE_APPLY=0

# 合规红线过滤开关：默认 1=开启（中国部署必须过滤政治/地缘/军事敏感市场）；
# NB 省部署（无合规风险）设 0 即可整体关闭，交易所有市场
# COMPLIANCE_FILTER=1

# 实盘模式开关：1=把策略意图挂单真实发到 CLOB（需 PM_BOT_PK）；默认 0=DRY_RUN 仅模拟
# 仅 NB 部署、且已填 PM_BOT_PK 时设 1。北京环境务必保持 0。
# LIVE_MODE=0

# 真实成交率轮询间隔（秒）：LIVE_MODE=1 时后台每 N 秒查一次在途订单真实成交状态
# LIVE_POLL_SEC=30

# 钱包私钥（仅 NB 实盘用，从环境变量读取，绝不入 git/日志）：LIVE_MODE=1 时必填
# PM_BOT_PK=

# 其余模拟盘环境变量见 DEPLOY_POLYMARKET.md：SIM_MODE / FILL_BASE / PRICE_REFRESH_SEC / AUTO_REPORT_MIN 等
