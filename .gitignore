# Python
__pycache__/
*.pyc
.venv/
venv/
env/

# 密钥（切勿提交）
.env

# 日志与缓存
*.log
webui.err
data/cache/
a_share/data/cache/
a_share/data/alt/
pip_install*.log

# 自动荐股缓存（运行时生成，不入库）
a_share/recommend_cache.json
a_share/xsec_recommend_cache.json

# 生成的看板产物（每次运行重生成，不入库）
output/dashboard.html
output/*.html

# 系统
.DS_Store
Thumbs.db

# WorkBuddy 会话数据（本地，不入库）
.workbuddy/

# 模拟交易账本（运行时状态，含测试成交，不入库）
a_share/sim_book.json
a_share/arb_book.json
# Polymarket 模拟盘账本与日志（运行时状态，不入库）
a_share/sim_book_poly.json
a_share/sim_logs/

# 信号持久化状态（每日重算生成，含 as_of 时间戳，不入库）
a_share/signal_state.json
run_daily_report.bat
a_share/crypto_watchlist.json

# 模拟盘看板运行时数据（成交流水/权益序列/检查点，累积式生成，不入库）
a_share/data/
a_share/sim_book_dryrun_test.json
a_share/live_dryrun_orders.jsonl
a_share/live_reconcile.jsonl

# 本地启动脚本（含本机路径，不入库）
start_sim_dashboard.bat
重置模拟数据.bat
