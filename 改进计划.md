# Quant-Trading 改进计划（基于 Nautilus Trader 对比）

> 版本：v1 ｜ 拟订：2026-08-23 ｜ 适用分支：master（Gitee `origin` + GitHub `github` 双远端同步）

---

## 0. 背景与总原则

- **对比对象**：`nautechsystems/nautilus_trader` —— 生产级 Rust 原生多资产多场所**实盘**引擎（crypto/FX/股票/期货/期权/预测市场/博彩/RWA）。
- **我们定位**：免费自部署**研究/模拟**工具，虚拟盘、不涉真实资金；Python 单文件 `http.server`，代码约定用 `urllib`（不引 `requests`）。
- **总原则**：**抄设计思想，不抄工程重量**。Nautilus 是机构级重装备，我们只取其可落地的架构思想，绝不引入 Rust 核心、纳秒撮合、实盘框架等过度工程。
- **已存在的基础（别重复造轮子）**：
  - `a_share/datasource.py` —— 数据源抽象**雏形**（Phase 4 直接固化）
  - `a_share/backtest_engine.py` —— 已含佣金/印花税/滑点（Phase 5 直接抽离）
  - `a_share/kalshi.py` —— 第二个预测市场适配器（说明多源整合已在发生）
  - `a_share/arb_book.py` / `a_share/arb_backtest.py` / `a_share/sim_engine.py` —— 三处各自实现撮合/盈亏（**Phase 1 的核心痛点**）

---

## 1. Nautilus 对比结论速览

| 维度 | 我们 Quant-Trading | Nautilus Trader | 对我们的启示 |
|------|-------------------|-----------------|--------------|
| 定位 | 研究/模拟，虚拟盘 | 生产级实盘引擎 | 不碰 live 框架 |
| 技术栈 | Python 单文件 | Rust 核心 + Python(PyO3) | 不引 Rust |
| 架构 | 请求-响应 WEB 仪表盘，模块分散 | 事件驱动 + 消息总线 + Cache + Portfolio + Clock；回测/实盘同构 | 借"逻辑统一 + Portfolio 层" |
| 资产 | A股 + Polymarket + 加密 + Kalshi | 同上 + 更多 | 已有多源，需统一视图 |
| 回测 | 分钟/日频，单标的 + 配对做市 | 纳秒/tick，确定性 | 频率够用，不追 tick |
| 数据 | akshare/tushare/Binance/Gamma-CLOB | 20+ 适配器 | 适配器模式可固化 |

**可借鉴三档**：
- 🟢 **直接借鉴（能落地）**：①回测/模拟盘策略逻辑统一 ②跨资产 Portfolio 层 ③配置驱动参数外置 ④可插拔撮合/费用模型 ⑤数据源适配器模式
- 🟡 **裁剪参考**：轻量事件总线（仅做自动轮动+推送时需要）
- 🔴 **不适用/过度工程**：Rust 核心、纳秒/tick 撮合、IOC/FOK/OCO 高级订单、实盘 live 框架、分布式多节点

---

## 2. 改进路线图（分步，按优先级从高到低）

### Phase 1 —— 策略逻辑统一抽象（最高优先 · 低风险）🟢

**目标**：消除 `arb_backtest.py`（历史回测）、`arb_book.py`（虚拟盘做市）、`sim_engine.py` 三处各自实现的撮合与盈亏计算，抽到共享核心。

- **痛点**：三套各写撮合/盈亏，费率扣法、未实现盈亏口径易漂移，改一处忘一处。
- **改动文件**：
  - 新建 `a_share/core/strategy.py`（或 `strategy_lib.py`），包含：
    - `FillModel`：给定买/卖价、`half_spread`、`fee_rate`、`size` → 成交价与手续费
    - `FillModel.realized_pnl(avg_cost, price, size, buy_fee, sell_fee)`
    - `FillModel.unrealized_pnl(mid, size, avg_cost)`
    - `portfolio_reval(positions, mids) → equity`
  - `arb_backtest.py` / `arb_book.py` / `sim_engine.py` 改为调用共享核心，删除重复实现。
- **验收标准**：同一组输入下，抽取前后回测/模拟盘的盈亏数字完全一致；重复代码删除。
- **风险**：低（纯重构，可用现有手动回测 + 滑块 sweep 对比验证）。

### Phase 2 —— 配置驱动参数外置 🟢

**目标**：所有策略参数集中到配置文件，改策略不动代码。

- **现状**：回测面板已有 `half_spread`/`fee` 滑块 + 🔍参数扫描矩阵（`webui.py`）；但其余策略参数仍硬编码。
- **改动**：
  - 新建 `config/strategies.yaml`（或 `.json`）；`webui.py` 读取；滑块值回写配置。
  - 把 `days`/`every`/`size`、各市场费率、扫描网格等全部外置。
- **验收标准**：调 `half`/`fee`/`size`/`days`/网格 只改配置即生效，无需改代码。
- **风险**：低。

### Phase 3 —— 跨资产 Portfolio 层 🟢

**目标**：统一 A股（akshare/tushare）、Polymarket（`polymarket.py`）、Kalshi（`kalshi.py`）、加密 的持仓与盈亏视图。

- **现状**：三方各自为政；`datasource.py` 已有数据源抽象雏形可复用。
- **改动**：新建 `a_share/portfolio.py` —— 聚合多源 `positions`，输出合并权益曲线 + 分项占比。
- **验收标准**：单页看到全部资产净值与各自盈亏；对接"整合多项目/多数据源"诉求。
- **风险**：中（需先统一各源持仓数据结构，建议与 Phase 1 的 `strategy_lib` 配合）。

### Phase 4 —— 数据源适配器模式（已起步，固化）🟢

**目标**：把 `datasource.py` 的抽象正式定为统一 `DataSource` 接口。

- **现状**：`datasource.py` 已存在；`kalshi.py`、`polymarket.py`、`tushare_adapter.py` 各自实现。
- **改动**：定义 `DataSource` 基类（`fetch_quotes()` / `fetch_history()`）；让上述适配器继承；新增源只写适配器。
- **验收标准**：加一个新数据源只需新增一个适配器文件，`webui.py` 零改。
- **风险**：低。

### Phase 5 —— 可插拔撮合/费用模型 🟢

**目标**：`backtest_engine.py` 的佣金/印花税/滑点抽成 `FillModel` 注册表（与 Phase 1 的 `FillModel` 合并）。

- **现状**：`backtest_engine.py` 已含这些费用项，但散落 if/else。
- **改动**：按市场类型注册费用模型（A股含印花税、Polymarket 含单边撮合费、加密含 maker/taker），运行时按源选择。
- **验收标准**：不同市场自动套用各自费用模型，无散落分支。
- **风险**：低。

### Phase 6（延后）—— 轻量事件总线 / 自动化流水线 🟡

- **仅当**要做"自动轮动 + 手机推送"时才上。用 Python `asyncio` 或简单 pub/sub，**不上 Redis/Rust**。
- **现状**：`notify.py` 已在做推送。
- **触发条件**：老吴确认要做自动轮动流水线。
- **风险**：中。

---

## 3. 明确不做（避免过度工程）🔴

- ❌ Rust 核心（收益不抵复杂度，纯模拟池子小）
- ❌ 纳秒/tick 级撮合（日/分钟频已够）
- ❌ IOC/FOK/OCO/iceberg 高级订单（模拟盘 + A股用不上）
- ❌ 实盘 live 交易框架（不涉真实资金 + 合规只做虚拟盘）
- ❌ 分布式/多节点（Nautilus 自己都说 UI 超出范围，我们 `webui.py` 够用）

---

## 4. 当前进度基线（已完成，勿回退）

- ✅ 回测参数滑动条（`half`/`fee`）+ 🔍参数扫描矩阵（5×5，盈利绿/亏损红）
- ✅ 回测权益曲线 + 账户权益曲线 导出 PNG/CSV
- ✅ `fetch_price_history` 直查 CLOB `prices-history`（Polymarket 历史唯一可用源）
- ✅ 套利模拟器使用手册升 v5
- ✅ Gitee（`origin`）+ GitHub（`github`）双远端同步，共 60 commits

---

## 5. 给另一台电脑的第一件事

从 **Phase 1** 开始：
1. 先读 `a_share/arb_backtest.py` 与 `a_share/arb_book.py`，摸清两边撮合/盈亏实现的差异；
2. 抽 `a_share/core/strategy.py`，把 `FillModel` / 盈亏 / 组合重估统一进去；
3. 让 `arb_backtest.py` / `arb_book.py` / `sim_engine.py` 改调共享核心，删重复实现；
4. 用现有滑块 sweep（5×5 矩阵）验证抽取前后盈亏一致；
5. **每完成一个 Phase 就 commit + 推双远端**（Gitee `origin` / GitHub `github`），保持两端一致。

---

## 附：相关已有文档

- `对比分析_tickflow.md`（tickflow 对比调研）
- `套利模拟器使用手册.md`（v5 使用手册）
- `备忘录.md` / `策划文档.md`（项目备忘与策划）
