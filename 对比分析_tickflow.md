# 对比分析：tickflow-stock-panel vs 我们的 Quant-Trading

> 调研日期：2026-08-22
> 对象仓库：https://github.com/shy3130/tickflow-stock-panel （~3.4k star，活跃迭代）
> 目的：判断是否值得借鉴，及借鉴优先级。

## 一、它是什么

一个**自托管、零运维的 A 股「选股 + 监控 + 回测」工作台**。设计哲学鲜明：**"拒绝荐股"**——
LLM 只做描述/起草（生成策略、四维分析、盘后复盘），从不说"买哪只"。与我们"只给信号不替你下单"一致。

技术栈（很"当代"）：

| 层 | 选型 |
|---|---|
| 后端 | FastAPI + Pydantic v2 + APScheduler + sse-starlette |
| 数据 | **Polars（计算）+ DuckDB（查询）+ Parquet（存储）** |
| 回测 | vectorbt（全项目唯一 pandas 边界） |
| 前端 | React 18 + Vite + TS + Tailwind + Lightweight Charts |
| 部署 | Docker 单容器 / `dev.sh` |

七大模块：选股引擎（18 内置策略 + 自定义 + AI 生成，Polars 毫秒扫全 A）、指标流水线（MA/EMA/MACD/RSI/KDJ/布林落盘 Parquet）、回测（T+1/手续费/滑点/止损，SSE 流式进度）、监控中心（4 类规则 AND/OR + 飞书推送 + 语音播报）、个股分析（日K+9类关键价位+AI四维）、连板梯队、数据扩展。

## 二、硬碰硬对比

| 维度 | tickflow-stock-panel | 我们的 Quant-Trading |
|---|---|---|
| 市场覆盖 | **仅 A 股** | **A 股 + 加密双线**（ccxt dry-run）✅ |
| 选股性能 | Polars 向量化，毫秒扫全 A | pandas，五板块 39 只池 ⚠️（池子小，不构成瓶颈） |
| 回测 | vectorbt，**含 T+1/手续费/滑点/止损 + 净值/夏普/回撤/胜率** ✅ | 仅有信号准确率回放 + 横截面多空草稿，**缺可复用单标的撮合回测** ❌ |
| 监控 | 4 类规则 AND/OR + 飞书 + 语音 | 仅钉钉推送，无规则引擎 ⚠️ |
| 数据源 | TickFlow + stock-sdk(免费) + 同花顺概念 | 腾讯/东财/新浪多源 + Tushare + 合成数据兜底 ✅ |
| AI 层 | LLM 策略生成（AST 白名单）+ 四维分析 | 规则四维信号 + ML(LR/GBDT) 回测，无 LLM |
| 深度因子 | 监控为主 | **L2 订单流 12 维 + 资金流** ✅ 更深 |
| 安全/鉴权 | PBKDF2 + HttpOnly + 防抢占 + 防 XFF 伪造 ✅ | **stdlib http.server 零鉴权** ❌（暴露公网即裸奔） |
| 部署 | Docker 单容器 | venv + .bat |
| 推送渠道 | 飞书 | 钉钉 |

## 三、借鉴优先级（按性价比）

1. **回测流水线（最该抄，已完成优先改善）**——把 T+1、手续费、滑点、止损做成回测默认约束。
   我们此前只有"信号准确率"和横截面多空草稿，缺"信号→历史真实撮合→净值/夏普/回撤/胜率"的
   可复用单标的引擎。已新增 `a_share/backtest_engine.py`（纯 pandas，零新依赖，带 A 股真实摩擦）。
2. **webui 安全加固（最该补的隐患）**——当前 8787 端口零鉴权；其 PBKDF2 + HttpOnly + 防抢占
   + 防 XFF 伪造是现成范本，建议照搬（至少加访问口令）。
3. **Polars/DuckDB 替换 pandas 选股层**——**现阶段不值得抄**：我们池子 39 只，pandas 不构成瓶颈；
   Polars 是给"全市场扫描"用的，现在上是过度工程。等要扫全 A 股做横截面选股时再迁。
4. **监控中心 + 多推送渠道**——其 4 类规则 AND/OR + 飞书/企微 + 语音播报比"只钉钉一下"强；可加飞书第二通道 + 规则 UI。
5. **SSE 流式进度**——长任务（选股/回测）面板目前阻塞，加 SSE 体验直接上一级。
6. **连板梯队 / 情绪雷达**——A 股特色模块（连板层级、概念轮动、炸板预警），我们完全没有。
7. **LLM 四维分析 + AST 白名单**——若以后加"AI 读单只票"，其 AST 白名单（只放行 polars/__future__，禁 eval/exec）是安全范本。

## 四、我们比它强的（别乱抄把优势丢了）

- **加密线**：它纯 A 股，我们有 ccxt 模拟盘——差异化资产，保留。
- **L2 订单流 + Tushare 资金流**：它监控浅，我们因子更深。
- **合成数据兜底 + 推送安全闸门**（离线不推假信号）——它没这层克制。
- **钉钉集成**：国内散户群用钉钉比飞书普遍，保留。

## 五、结论

值得借鉴，但**不用 fork 它**。最划算的动作：**先补回测流水线和 webui 鉴权**（两个真短板），
再把它的 Polars 选股层、监控中心、连板梯队当增量模块嫁接。选股性能（Polars）暂不抄。
