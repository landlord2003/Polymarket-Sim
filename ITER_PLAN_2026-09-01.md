# 迭代计划（2026-09-01，按重要度排序，逐项打勾）

> 背景：今日完成「成果推送 GitHub + NB 完整部署交接」后，进入迭代阶段。
> 本计划为一个一个来、每项完成打勾。代理支持已剔除（NB 直连无需、北京无外网测不了）。
> 任务追踪：TaskList #139 / #140 / #141（✅）/ #142 / #143（pending）。

---

## ✅ #139 完整部署安排交接
- **目标**：把平台完整部署与运维步骤交接到 NB 伙伴，零隐式坑。
- **做了**：`DEPLOY_NB.md`（NB 部署全步骤）+ `HANDOFF_NB.md`（9 阶段勾选清单）+ `DEPLOY_POLYMARKET.md` / `USAGE_MANUAL.md` 同步 + `start_nb.py` 一键启动器 + systemd 守护 + `.env.nb` 模板。
- **状态**：✅ 已完成（commit `231ff46`）。
- **交付文件**：`DEPLOY_NB.md`、`HANDOFF_NB.md`、`start_nb.py`、`polymarket-sim.service`、`.env.nb`。

## ✅ #140 钉钉推送频率 2 小时
- **目标**：周期报告从默认 30 分钟降为 2 小时（用户明确：不是任务完成时的推送）。
- **做了**：`sim_server.py` 的 `AUTO_REPORT_MIN` 默认 `30 → 120`；`DEPLOY_NB.md` / `DEPLOY_POLYMARKET.md` 文档同步。
- **状态**：✅ 已完成（commit `3a39e01`）。
- **注意**：独立的 `sim_pipeline.py --runs 1 --push-dingtalk`（每 6h 跑完整流水线+反馈）用途不同，不在此 2h 内。

## ✅ #141 风控引擎增强（自动熔断 + 看板红灯）
- **目标**：组合级自动熔断（日亏/回撤/本金下限）+ 看板红灯，接真钱前硬前置。
- **做了**：
  - `risk_control.evaluate_portfolio_guard()`：日亏 `DAILY_LOSS_LIMIT` / 回撤 `DRAWDOWN_LIMIT=0.15` / 本金下限 `BANKROLL_FLOOR_FRAC=0.70` 任一触限即自动 `trigger_kill_switch` + 钉钉告警，已熔断幂等；
  - `sim_server.risk_monitor_loop()` 守护线程（每 `RISK_CHECK_SEC≈30s` 巡检）；
  - `/api/state` 增 `risk_guard`、`/metrics` 增 `polymarket_sim_risk_guard`、看板 `#riskbadge` 转红 + 顶部横幅；
  - **修两个真 bug**：`threading.Lock`→`RLock`（持锁调 kill switch 死锁）、`_guards` 补 `reason`；
  - 新增 `test_risk_guard.py`（7 项全过）。
- **状态**：✅ 已完成（commit `a5d8a52` + 文档 `dee2c9a`）。

## ✅ #143 LP-reward 感知定价
- **目标**：把 Polymarket LP 的奖励半宽 δ 纳入做市报价（双边收益 = 价差 + 持币时间加权奖励），模拟验证"双收益"是否优于当前纯价差模型。
- **做法**：
  - 新增 `lp_reward.py`：奖励区 `[mid-δ, mid+δ]`、`in_band()` 判定、`lp_reward_quote()`（对比「纯价差」vs「区内挂单」blended edge 并自动择优）、`compare_over_quotes()`、`sweep()` 参数扫描。纯函数、零网络。
  - 接入 `arbitrage.scan_poly_marketmaking`：每个 MM 机会挂 `lp_reward` 字段（奖励区/双边是否在带内/blended edge/建议 half_spread）；`LP_REWARD_DELTA`/`LP_REWARD_APR`/`LP_REWARD_TIME_H`/`LP_REWARD_ON` 环境变量可调。
  - 新增 `backtest_lp_reward.py`：对 `data/quotes_ts/*.jsonl`（1316 帧真实盘口快照）walk-forward 对比，输出 `LP_REWARD_BACKTEST.md`。
  - `sim_server` 新增只读 `/api/lp_reward` 端点（NB 可实时 curl 看双收益 lift）。
  - 新增 `test_lp_reward.py`（8 项全过）。
- **验证结论（诚实）**：保守假设 δ=1%/apr=20% 下，双收益相对纯价差平均 **+2.53%**（薄价差市场奖励占优、深价差市场自动退回纯价差）；在 lp_tool 实测高奖励率（apr≈365%）下 lift 达 **+50%**。**即 #143 价值完全取决于真实 apr 高低**——这直接决定"30% 盈利"能否兑现。δ/apr 仍为假设值（北京无外网），NB 有网回填真实值。
- **状态**：✅ 已完成（见下方 commit）。

## ⬜ #142 开源 README 整理
- **目标**：GitHub `landlord2003/Polymarket-Sim` 公开准备——把面向外部的 README 写清楚（项目是什么/能做什么/不能做什么/如何部署/如何接真钱/免责声明）。
- **做法**：基于现有 `README.md`（偏 A股线，需补 Polymarket 模拟盘专章）+ `POLYMARKET_ONBOARDING.md` + `DEPLOY_NB.md` 整合一版对外 README。
- **状态**：⬜ 待启动（排在 #143 之后）。

---

## 进度速查
| # | 项 | 状态 | commit |
|---|----|------|--------|
| 139 | 完整部署交接 | ✅ | 231ff46 |
| 140 | 钉钉 2h | ✅ | 3a39e01 |
| 141 | 风控增强 | ✅ | a5d8a52 |
| 143 | LP-reward 定价 | ✅ | <pending> |
| 142 | 开源 README | ⬜ | — |
