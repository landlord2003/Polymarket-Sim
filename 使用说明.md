# 量化交易系统 · 使用说明

> 主导：老吴（吴自强） ｜ 协办：Claw
> 总原则：**双线并行（A股 + 加密）· 全模拟起步 · 零资金 · A股只给信号、不自动下单（手动在券商 App 执行）**

这份说明面向「要上手用」的人，不讲架构，只讲：**每个功能是什么、什么时候该用它、怎么操作**。
技术细节（四维度打分逻辑、调参字段）见 `README.md`，另一台电脑部署见 `DEPLOY.md`。

---

## 一、功能速览（什么时候用哪个）

| 功能 | 入口 | 一句话用途 | 最适合的场景 |
|------|------|-----------|-------------|
| 🖥️ 可视面板 | `启动看板.bat` → 浏览器 | 页面上点按钮就能扫描、自动刷新、实时报价 | **每天盯盘主力入口，不想敲命令时用** |
| 📊 日常盯盘信号 | 面板按钮 / 命令行 | 自选股四维度打分 → 买/卖/等信号 | 想知道「我盯的票现在该不该动」 |
| 🔎 五板块选股 | 面板按钮 / 命令行 | 新能源/新材料/AI/机器人/军工 批量挖新标的 | 自选不够、想找新标的建仓 |
| 📄 静态 HTML 看板 | 命令行生成 | 自包含深色大屏，可存档/转发 | 想离线看、或面板起不来时备用 |
| ⌨️ 命令行 `run_daily.py` | 终端 | 跑信号 + 生成日报 + 推送钉钉 | 想设定时任务、手机收信号 |
| 📱 钉钉/企微推送 | `.env` 配置 | 信号日报推到手机 | 不下面板也想随时看 |
| 🪙 加密模拟盘 | `crypto/bot_dryrun.py` | 零资金验证加密交易技术栈 | 想研究加密、但不碰真实资金 |
| 🔌 数据源直连 | `datasource.py` | 多源取行情（腾讯/东财/新浪） | 取数失败排查、想加新数据源 |
| 🚀 双击启动器 | `启动看板.bat` | 一键开面板（清端口+选 python+开浏览器） | 每次想开面板时 |
| 🛡️ 风控闸门 | `risk/risk_control.py` | 回撤/仓位/止损/熔断，自动作用 | 不用手点，全程后台生效 |

---

## 二、各功能详解

### 1. 🖥️ 可视面板（webui）—— 日常最常用

**这是什么**：一个本地网页，双击就能开，不用敲任何命令。所有扫描都在页面上点按钮触发，结果自动刷新。

**怎么打开**：
1. 进入 `quant-trading` 文件夹
2. **双击 `启动看板.bat`**
3. 浏览器自动打开 `http://127.0.0.1:8787`（窗口别关，关了服务就停）

> 启动器会先做两件事：① 清掉占用 8787 端口的旧进程（避免旧代码糊弄你）；② 自动选 python（项目 `.venv` → 本机 WorkBuddy 托管环境 → 系统 python）。

**面板里有什么**：
- 顶部三个按钮：**📊 日常盯盘**（跑自选股信号）/ **🔎 板块选股**（五板块挖新标的）/ **🚀 全部运行**（两者一起跑）
- 两个勾选：**☐ 离线验证**（用合成数据跑顺、不联网不推送，适合先试流程）/ **☐ 推送钉钉**（联网且配好 `.env` 时把日报推到手机）
- **🔄 自动刷新**：勾上后按选定间隔（1/3/5/15 分钟）自动重跑「最近一次点的模式」；状态栏有下次刷新倒计时；默认勾 **仅盘中**（非交易时段不空跑，想强制刷新就取消勾）
- **实时报价条**：第二行常驻显示全部自选股当前价和涨跌幅（**涨红跌绿**，A股惯例），每 10 秒自动刷新，走轻量快照、不跑引擎
- **交易时段徽标**：显示 集合竞价 / 盘中 / 午间休市 / 已收盘 / 休市（周末）+ 服务器时间
- 下方信号看板，每只票都标注 **数据源 + 数据日期**：
  - `✅ 真实行情｜来源 腾讯财经(前复权)｜数据日 2026-08-18` → 真数据，可参考
  - `⚠️ 合成数据…｜回退原因：…` → 假数据，纯验证用，**不可据此下单**

**什么时候用**：每天开盘/盘中想看自选股信号；想挂着自动刷新盯盘；不想碰命令行。

---

### 2. 📊 日常盯盘信号（四维度打分）

**这是什么**：对 `watchlist.json` 里 7 只自选股，从 4 个维度打分后综合出一个交易信号。

| 维度 | 权重 | 看什么 | 取不到数时 |
|------|------|--------|-----------|
| 行情 | 35% | RSI 超买超卖、MA20 趋势、布林带位置 | 三源全败才回退合成并告警 |
| 资金 | 30% | 主力净流入额 + 连续天数 | 降级本地量价代理（涨/跌日成交额净比 + MFI14），仍给分并标「代理」 |
| 板块 | 20% | 个股 20 日收益 vs 沪深300 | 降级为个股自身 20 日动量 |
| 消息 | 15% | 东财新闻关键词情绪 | 置中性 0 分并注明原因 |

综合分 → **买入(≥0.5) / 偏多(≥0.15) / 观望 / 减仓 / 卖出**。所有信号过风控闸门后才输出。

**信号怎么读**：
- 🟢 **买入**：进入你设的建仓区间、或动态布林下轨企稳
- 🟡 **偏多**：趋势向好但未到买点
- ⚪ **观望**：无明显信号
- 🔴 **减仓 / 卖出**：触及阻力减仓、或（持仓时）跌破止损

**什么时候用**：每天决定「我盯的票现在该不该买/卖/等」。这是核心功能，配合面板的自动刷新，盘中随时看。

---

### 3. 🔎 五板块自动选股

**这是什么**：按 新能源 / 新材料 / AI / 机器人 / 军工 五个板块，联网取成分股 → 行情 + 量能异动初筛 → 每个板块 TopN 推荐。

**逻辑**：`sectors.json` 里每个板块有关键词（如「新材料」匹配 新材料/碳纤维/石墨烯/有机硅），联网命中板块后取成分股扫描。

**兜底**：若联网取不到板块列表，自动用内置 `CORE_POOL`（全是真实龙头股代码，如宁德时代、中航沈飞），**价格仍是真的**，看板会标「股池：本地核心池；价格：真实行情」，不误导成合成数据。

**什么时候用**：手头自选不够、想找新标的建仓；想换仓或拓展关注面时（建议每周跑一次）。

---

### 4. 📄 静态 HTML 看板（output/dashboard.html）

**这是什么**：命令行跑完扫描后生成的**自包含深色大屏**（零 CDN、无外部依赖），双击就能在浏览器打开。

**怎么生成**：
```bash
python a_share/run_daily.py              # 联网，生成看板 + 推送
python a_share/run_daily.py --no-html   # 只推送、不生成看板
```
文件落在 `output/dashboard.html`（已被 `.gitignore` 忽略，不进仓库）。

**什么时候用**：想离线看大屏、或把当天结果存档/转发别人；面板起不来时的备用方案。

---

### 5. ⌨️ 命令行 run_daily.py

**这是什么**：面板背后其实就是它。命令行适合「不想开面板」或「想定时自动跑」的场景。

**参数**：

| 命令 | 作用 |
|------|------|
| `python a_share/run_daily.py` | 联网取数，真实信号 + 生成看板 + 推送钉钉/企微 |
| `python a_share/run_daily.py --offline` | 合成数据验证引擎，不推送 |
| `python a_share/run_daily.py --screener` | 额外跑五板块选股 |
| `python a_share/run_daily.py --screener --offline` | 选股离线自检 |
| `python a_share/run_daily.py --no-html` | 只推送、不生成 HTML |

**什么时候用**：
- 想设**定时任务**（Windows 任务计划程序每天 15:30 跑），把 `--no-html` 或带推送的版本挂上去，收盘后手机收日报
- 不想开面板、纯命令行工作流

---

### 6. 📱 钉钉 / 企业微信推送

**这是什么**：把信号日报推到你手机。凭证放在 `.env`，**绝不进仓库**（已被 `.gitignore` 排除）。

**配置**（复制模板后填值）：
```bash
cp .env.example .env
```
`.env` 里填：
- `DINGTALK_WEBHOOK` —— 钉钉机器人 Webhook 完整 URL（含 access_token）
- `DINGTALK_SECRET` —— 钉钉机器人加签密钥（SECxxx）
- `WECOM_WEBHOOK` —— （可选）企业微信机器人 Webhook

未配置时不会报错，只是跳过推送（方便离线验证）。

**什么时候用**：不想一直开着面板，也想收盘后手机收到完整信号日报。

---

### 7. 🪙 加密模拟盘（crypto/bot_dryrun.py）

**这是什么**：用 CCXT 走完整交易链路（取数 → RSI/EMA 信号 → 风控闸门 → 下单），**全程零资金**。

两种零风险模式：
1. `dry_run=True`（默认）：永不真实下单，只模拟「会下什么单」，验证整条技术栈
2. `dry_run=False + testnet=True + 配 API Key`：向 Binance Testnet 下真实测试单（测试资金，零真实风险）

**用法**：
```bash
python crypto/bot_dryrun.py --once              # 跑一轮（默认离线合成，验证逻辑）
python crypto/bot_dryrun.py --once --live       # 连 Binance/Testnet 实跑一轮
python crypto/bot_dryrun.py --loop --interval 60  # 每 60 秒一轮（Ctrl+C 退出）
```
变量在 `.env`：`CRYPTO_SYMBOL` / `CRYPTO_TIMEFRAME` / `CRYPTO_TESTNET` / `CRYPTO_DRYRUN` / `CRYPTO_API_KEY` / `CRYPTO_API_SECRET` / `CRYPTO_PUSH`。

**什么时候用**：想研究/验证加密交易技术栈。⚠️ 境内访问交易所需合规自担；本脚本仅做技术验证，不鼓励实盘。

---

### 8. 🔌 数据源直连（datasource.py）—— 技术背景

**为什么不用 akshare 取行情**：2026-08-19 实测 `akshare.stock_zh_a_hist` 被东方财富（`push2his.eastmoney.com`）以 `RemoteDisconnected` 掐断（服务端 WAF/限流），而腾讯、新浪均秒回。原引擎静默回退合成数据，导致取消「离线验证」后看到的还是假数。

现由 `datasource.py` 统一直连（仅标准库 `urllib`，无 requests 依赖）：
- `fetch_kline`：腾讯前复权 → 东财前复权 → 新浪不复权，每源 3 次重试退避（抗东财间歇限流）
- `fetch_realtime`：新浪批量快照，一次拿全部自选股当前价
- `fetch_money_flow` / `fetch_index_kline`（沪深300）/ `fetch_news_titles`
- `market_phase`：交易时段判断
- 真实来源写入 `df.attrs['source']`；全部失败时把原因写进 `fallback_reason` 并在看板显示 —— **绝不静默伪装成真实行情**

自检：`python a_share/datasource.py`（可带股票代码参数）。

**什么时候关心它**：取数失败时在看板看「回退原因」判断是网络还是限流；想接入新的行情源时改这里。

---

### 9. 🚀 双击启动器（启动看板.bat）

**是什么**：一键启动面板的 Windows 脚本。

**流程**：① 清掉占用 8787 的旧进程 → ② 自动定位 python → ③ 启动 `webui.py` 并自动开浏览器。

**什么时候用**：每次想开面板都双击它，不用敲命令、不用怕旧进程干扰。

---

### 10. 🛡️ 风控闸门（risk/risk_control.py）

**是什么**：双线共用的风控模块，对每笔信号做回撤/仓位/止损/熔断检查。

**怎么用**：不用手点，引擎在输出信号前自动过这道闸门（如最大回撤熔断、单票仓位上限）。信号被拦截时看板/日报会注明原因。

---

## 三、你最常改的配置文件

所有调参集中在配置文件，**改一处即可，不用动代码**。

### watchlist.json（自选股 + 个性化规则）

顶层字段：
- `holding`：`false`=空仓（规则按「回补参考」解读）/ `true`=持仓（止损跌破才报卖出）。**当前全空仓，保持 `false`**
- `weights`：四维度权重 `{market:0.35, money:0.30, sector:0.20, news:0.15}`，一处调全局

个股 `rules`（引擎强制优先级：止损 > 动态布林/建仓区间 > 阻力减仓）：
- `buy_range: [lo, hi]` —— 价格落入区间 → 强制 🟢买入（空仓视为回补参考）
- `use_dynamic_boll: true` —— 启用**动态布林下轨买点**（每次按实时 20日−2σ 算，不写死历史价）；`boll_tol` 容差默认 0.03
- `stop_loss: 价格` —— 跌破 →（空仓）偏弱不接飞刀 /（持仓）🔴卖出
- `resistance: 价格` —— 触及 → 提示减仓/止盈
- `cost_avg`（可选）—— 仅备注，空仓无需填

### sectors.json（板块关键词）
`scan_sectors` 里每个板块的 `candidates` 是板块名模糊匹配词，想加板块或改关键词改这里。

### .env（推送凭证）
见上文「钉钉/企微推送」。

**常见改动示例**：
- **加一只自选股**：在 `watchlist` 数组里加一个 `{symbol, name, sector, tags, note, rules}` 对象
- **改建仓区间**：改对应票的 `rules.buy_range`
- **改权重**：改顶层 `weights`

> ⚠️ 钢研的「18.45」是早期价格较高时的布林下轨**快照**，现价已随价格下移，故用**动态布林下轨**，不再写死。悦安/万丰的 `rules` 为 **2026-08-19 技术参考值**，非真实成本，可按你的回补价/破位线调整。

---

## 四、推荐日常流程（全空仓版）

1. **每天**：双击 `启动看板.bat` 开面板 → 勾「🔄 自动刷新」+「仅盘中」挂着
2. 看**实时报价条**（每 10 秒跳动）和**信号看板**，等你的买点：
   - 钢研：回**动态布林下轨企稳**
   - 元力：回 **25.5 建仓区间**
   - 悦安/万丰：回各自建仓区间 / 动态布林下轨
3. 出现 🟢买入 且价格进了你设的区间 → **手动在券商 App 下单**（工具不替你买）
4. **每周**：点一次「🔎 板块选股」挖新标的，扩充关注池
5. **想手机收**：配好 `.env` 钉钉，面板勾「推送钉钉」或命令行挂定时任务

---

## 五、常见问题 / 故障排查

| 现象 | 原因 | 处理 |
|------|------|------|
| 面板点了没反应 | 8787 上有旧进程跑旧代码 | 关掉面板窗口，**重新双击 `启动看板.bat`**（会自动清旧进程） |
| 取消离线后还是合成数据 | 取数失败（东财限流/网络） | 看板会标 `⚠️ 合成数据｜回退原因：…`；等一会重扫，或勾回「离线验证」先跑通流程 |
| 取数很慢 | 首次联网拉 320 天日K | 耐心等 30–90 秒，页面每 2 秒轮询 |
| 推送没收到 | `.env` 没配 / 密钥错 | 检查 `DINGTALK_WEBHOOK` 和 `DINGTALK_SECRET` 是否填对 |
| 想后台常驻 | 关终端就停 | `pythonw a_share/webui.py` 或 Windows 任务计划程序 |

---

## 六、安全与合规提醒

- 🔴 **零资金**：默认 dry-run / 纯回测 / 信号推送，不落真实订单
- 🔴 **A股不自动下单**：信号推给你，手动决策（自动下单需 QMT/PTrade，50万+ 门槛，P4 阶段再谈）
- 🔴 **密钥不入库**：`.env` 已被 `.gitignore` 排除，钉钉/企微凭证只在本地
- 🟡 **回测 ≠ 实盘**：小资金实盘前需样本外 + 模拟盘交叉验证
- 🟡 **GitHub 访问不稳**：克隆第三方仓库用镜像兜底（kkgithub / ghproxy）
- 🟡 **加密合规**：境内访问交易所需合规自担，本系统加密线仅做技术验证
