# Polymarket 调研报告 · 项目方意见（WorkBuddy ｜ 2026-08-31）

> 配套文件：`Polymarket完整报告_2026-08-31.md`（由 Claw 在另一台机器整理，2026-08-31）
> 本文件为本项目（Quant-Trading / Polymarket 模拟盘 v3）维护方对报告的研判与后续行动建议，不修改原报告内容。

---

## 一、一句话结论

报告方向正确，与本项目现状高度吻合。**核心边界维持不变**：北京 IP 下只做 `DRY_RUN` 模拟盘 + 只读数据；实盘真钱须落在境外开放司法辖区（VPS 或人物理抵达开放省）。报告给的 API / `py-clob-client` SDK / WebSocket 骨架，可直接对齐我们现有的 `mm_leg` / `caifu` pipeline。

---

## 二、与本项目现状的对齐（已验证）

- 我们 v3 已接 **CLOB 做模拟做市 + 三重合规过滤**（政治 / 地缘 / 军事 / 中东航运咽喉），与报告 §3 / §6 一致。
- 合规红线（中国部署过滤敏感市场）与报告 §4.4 互证，维持不动摇。
- 本轮刚补完的 P0-A/B/C（关停端点鉴权 / 持久化可观测 / 报告自动推钉钉）正好对应报告 §5.1「风控层 = kill switch + 告警」——我们已有雏形，且已实活验证（403 拦截、钉钉 errcode=0）。
- 服务当前运行实例 `R0JJng`（SIM_MODE=inv），`/api/fill_calibration` 实测 262 市场 `recommended_base=0.94`，`/api/shutdown` 无/错 token 均 403。

---

## 三、我补充 / 修正的判断

1. **报告把「NB 省本机实盘」写成可行路径——但对你是假设态。**
   你常驻北京（项目 memory 确认）。NB 路径仅在物理抵达开放省时成立，当下不可操作。近期唯一可落地的「实盘前置」是**境外 VPS**（报告 §5.1：MB/SK/韩国首尔等开放节点，$5–15/月）。不要把 NB 当成近期动作。

2. **WebSocket 是真实缺口。**
   我们 DRY_RUN 现用 **Gamma REST 轮询**（`PRICE_REFRESH_SEC`）。报告 §3.4 明确 WS 毫秒级对做市/套利「必需」。模拟盘无所谓；一旦转实盘，必须上 CLOB WebSocket 订阅。建议列入 P3 前置。

3. **L2 签名 + 私钥管理是从 0 到 1 的跃迁。**
   当前只接 Gamma 只读行情，无下单。实盘需 `py-clob-client` 派生 `api_key/secret/passphrase`（L2-HMAC）+ 钱包私钥 EIP-712 签名。钱包私钥**绝不可入 git / 代码**（报告 §5.1 已强调；我们 `.gitignore` 已挡 `.env`，但需新增 `PM_BOT_PK` / `PM_CLOB_*` 环境变量约定，并写进 `.env.example`）。

4. **缺金融风控层（这是转实盘的最大空白）。**
   我们有**合规过滤**，但无「单市场 / 总仓位上限 + 日亏损限额 + kill switch」的金融风控（报告 §3.6 / §5.1）。模拟盘不伤钱所以没做；转实盘前必须补齐，且 kill switch 要能**远程触发**（报告建议钉钉触发，我们已有钉钉通道，接一下即可）。

5. **只读数据价值可立即变现（北京可行，零合规风险）。🔴→🟢 关键机会**
   报告 §6 指出 Polymarket 实时概率（利率 / 宏观 / 经济类）是优质另类数据源。建议新增一个**只读探针**脚本：拉取非受限类市场概率 → 喂入低空情报 / 宏观监测工作流。这是当下北京就能做、无合规风险的高价值动作，应优先立项。

6. **受限地区清单时效性强。**
   报告 §4.3 / §8 自陈「监管状态每月变化」。任何真钱动作前，必须以 Polymarket 官方 Help Center **实时公告**复核受限地区（平台商业地理围栏，随监管与执法变动），勿直接采信本报告静态清单。

---

## 四、后续要做什么（建议优先级）

- 🟢 **立即可做（北京，零风险）**
  1. 只读探针脚本：Polymarket 概率 → 低空 / 宏观情报（给现有监测工作流供数）。
  2. 把本报告结论沉淀进 `NEXT_ITERATIONS.md`，新增 **P3「实盘前置」** backlog（境外部署 / WS / L2 签名 / 金融风控）。
- 🟡 **前置研发（不伤钱）**
  3. CLOB WebSocket 订阅模块（替代 Gamma 轮询，供未来实盘低延迟）。
  4. `py-clob-client` L2 接入 + 私钥 env 约定（仅本地 / 测试网演练，不碰真钱）。
  5. 金融风控层：单市场 / 总仓位上限 + 日亏损限额 + kill switch（钉钉可触发）。
- 🔴 **真钱关口（维持搁置，除非境外部署就位）**
  6. 境外开放省 VPS 部署 + 小资金（热钱包 $200–500）先模拟 ≥1 周再实盘。
  7. 保留全量交易记录，备税务 / 审计。
- ⏸️ **接真钱第三步**：维持你早前决策——按边界搁置，需境外部署 + $50–100。本报告未改变此边界（NB 仅是可选物理路径，非近期可操作项）。

---

## 五、需要你拍板的两件事

1. 是否现在就让**只读探针**脚本立项？（北京立即可用，给低空情报供数，零合规风险）
2. 是否把 **P3「实盘前置」** 写进 `NEXT_ITERATIONS.md`？（我可顺手提交，与本次报告一同入库）

> 附：原报告 §4.3 的受限地区清单为平台商业地理围栏，非主权表述；涉真钱前一律以官方实时公告为准。
