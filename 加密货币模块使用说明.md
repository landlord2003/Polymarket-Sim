# 加密货币模块使用说明

> 适用范围：本仓库 `crypto/` 目录下的「零资金加密模拟盘」全套功能。
> 配套阅读：仓库根 `使用说明.md`（A股四维度信号 + 面板）、`crypto/freqtrade/README.md`（框架版细节）。
> 最后更新：2026-08-20

---

## 一、这个模块是干什么的

加密模块是一条**「取数 → 信号 → 风控 → 下单」全链路验证工具**，核心目的是在**零真实资金**前提下验证技术栈（CCXT 接入、RSI/EMA 信号、风控闸门、模拟下单）。

三档零资金模式：

| 模式 | 是否需要密钥 | 是否真实下单 | 说明 |
|------|------------|------------|------|
| `dry_run`（默认） | 否 | 否 | 只模拟"会下什么单"，验证整条链路，**最安全** |
| `testnet`（Binance 测试网） | 是（测试网 key） | 是（测试资金） | 向 Binance Testnet 下真实测试单，用的是**测试资金**，仍零真实风险 |
| `freqtrade` 框架版 | 可选 | 可选 | 用专业框架接管策略/回测/Web UI，长期演进用 |

**合规提醒**：境内访问境外交易所需合规自担；本模块仅做技术栈验证，**不鼓励实盘**，绝不接真实资金账户。

---

## 二、文件清单

| 文件 | 角色 | 何时用 |
|------|------|--------|
| `crypto/bot_dryrun.py` | **主力脚本**（推荐日常用） | 想跑完整的「信号+风控+模拟下单」闭环 |
| `crypto/ccxt_demo.py` | 最轻量演示 | 只想看「CCXT 取数 + RSI 信号」最小可用 |
| `crypto/freqtrade/` | 完整框架版（含回测/绘图/Web UI） | 想长期演进、做回测和参数优化 |
| `a_share/notify.py` | 钉钉推送（被加密模块复用） | 打开 `CRYPTO_PUSH` 时每轮信号推钉钉 |
| `risk/risk_control.py` | 风控闸门（被加密模块复用） | 内置在 bot 里，无需单独调用 |

---

## 三、环境准备

### 3.1 Python 与依赖（已确认可用）

本机托管 venv 已装齐：`ccxt 4.5.74`、`numpy 2.5.2`、`pandas 3.0.5`。

- **本机（WorkBuddy 托管 venv）**：用
  `%USERPROFILE%\.workbuddy\binaries\python\envs\default\Scripts\python.exe`
- **你另一处 checkout `E:\Workbuddy\Quant-Trading`**：用 `.venv\Scripts\python.exe`

> ⚠️ 不要在普通 `python` 下跑——若那台机器没装 ccxt，需先 `pip install ccxt`。

### 3.2 `.env` 配置（项目根目录自建，已被 gitignore）

把下面内容存为 `quant-trading/.env`（与 `a_share/`、`crypto/` 同级的那个根目录）。**勿提交入库**。

```dotenv
# ---- 钉钉推送（A股与加密共用，可选）----
DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=你的token
DINGTALK_SECRET=你的加签密钥SECxxx
# WECOM_WEBHOOK=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxx

# ---- 加密货币模块环境变量（可选，均有默认值）----
CRYPTO_SYMBOL=BTC/USDT          # 交易对
CRYPTO_TIMEFRAME=1m             # K线周期
CRYPTO_TESTNET=true             # 是否走 Binance 测试网
CRYPTO_DRYRUN=true              # true=只模拟不下单；false=配合--live下测试单
CRYPTO_EQUITY=10000             # 模拟本金（美元）
CRYPTO_PUSH=false               # 是否推送钉钉
# CRYPTO_API_KEY=你的testnet_key      # 仅 testnet 真测试单时需要
# CRYPTO_API_SECRET=你的testnet_secret
```

> 不填任何 CRYPTO_* 也能跑：默认 `BTC/USDT / 1m / testnet / dry_run`，且缺密钥时自动转**离线合成数据**验证链路。

---

## 四、主力：`bot_dryrun.py` 用法

### 4.1 三条命令（从项目根目录运行）

```bat
:: ① 跑一轮即退出（默认离线合成数据，验证链路逻辑）
%USERPROFILE%\.workbuddy\binaries\python\envs\default\Scripts\python.exe crypto/bot_dryrun.py --once

:: ② 连 Binance / Testnet 实跑一轮（拉真实 K 线，但仍 dry_run 不下单）
%USERPROFILE%\.workbuddy\binaries\python\envs\default\Scripts\python.exe crypto/bot_dryrun.py --once --live

:: ③ 持续轮询（每 60 秒一轮，Ctrl+C 退出）
%USERPROFILE%\.workbuddy\binaries\python\envs\default\Scripts\python.exe crypto/bot_dryrun.py --loop --interval 60
```

### 4.2 下单行为矩阵（关键，别搞错）

| `--live` | `CRYPTO_DRYRUN` | 取数 | 实际动作 |
|---------|----------------|------|---------|
| 否 | true（默认） | 离线合成 | 只打印"会下什么单"，**零风险** |
| 是 | true | 真实 Binance | 只打印"会下什么单"，**零风险** |
| 是 | false | 真实 Binance | 向 **Testnet 下真实测试单**（测试资金） |
| 是 | false（且未配 testnet key） | 真实 Binance | 下单失败并提示缺密钥 |

> 想下 testnet 真测试单，必须三者同时满足：`--live` + `CRYPTO_DRYRUN=false` + 配置了 `CRYPTO_API_KEY/SECRET`（且 `CRYPTO_TESTNET=true`）。

### 4.3 输出解读（一行示例）

```
[14:03:21] BTC/USDT 价=61234.50 RSI=42.3 信号=hold(EMA金叉(快61400>慢61000) 且 RSI42 未超买) | ⚪ 持有，无操作
```

- **价**：最新收盘价
- **RSI**：14 周期相对强弱指标
- **信号**：
  - `buy`：EMA 快线 > 慢线（金叉）且 RSI < 70（未超买）
  - `sell`：EMA 快线 < 慢线（死叉）且 RSI > 30（未超卖）
  - `hold`：中性，无交叉
- **动作后缀**：
  - `[DRY-RUN]`：模拟下单，未真实成交
  - `[TESTNET]`：已下测试网真实测试单
  - `🔴 风控拦截`：被 `RiskController` 闸门挡下（见 4.4）
  - `[离线合成]`：本轮用的是合成 K 线

### 4.4 内置风控闸门（`RiskController`）

每次下单前过一道闸，参数固定为：
- `max_position_pct=0.20`：单笔最大仓位 20%
- `stop_loss_pct=0.05`：止损 5%
- `max_drawdown_pct=0.15`：最大回撤 15%

任一不满足 → 直接拦截并跳过下单，输出 `🔴 风控拦截：原因`。

### 4.5 环境变量速查表

| 变量 | 默认 | 含义 |
|------|------|------|
| `CRYPTO_SYMBOL` | `BTC/USDT` | 交易对 |
| `CRYPTO_TIMEFRAME` | `1m` | K线周期 |
| `CRYPTO_TESTNET` | `true` | 走 Binance 测试网 |
| `CRYPTO_DRYRUN` | `true` | 只模拟不下单 |
| `CRYPTO_EQUITY` | `10000` | 模拟本金（美元） |
| `CRYPTO_PUSH` | `false` | 钉钉推送开关 |
| `CRYPTO_API_KEY` / `CRYPTO_API_SECRET` | 空 | 仅 testnet 真测试单需要 |

---

## 五、最简演示：`ccxt_demo.py`

只想看「CCXT 接 Binance + RSI 信号」最小可用，跑这个：

```bat
%USERPROFILE%\.workbuddy\binaries\python\envs\default\Scripts\python.exe crypto/ccxt_demo.py
```

- 默认 `BTC/USDT / 1m`，走 Binance 测试网（sandbox）。
- 信号规则更朴素：`RSI < 30` → buy；`RSI > 70` → sell；否则 hold。
- **只打印，绝不下单**。适合第一次验证"网络 + 取数 + 指标"通不通。

---

## 六、框架版：Freqtrade（长期演进）

`crypto/freqtrade/` 是完整框架版，自带回测、绘图、参数优化、Web UI，适合把策略长期实盘化演进。

```bash
pip install freqtrade
# 配置 .env 里的 CRYPTO_API_KEY / CRYPTO_API_SECRET（testnet）
# 先 dry-run（不碰真钱）：
freqtrade trade --config user_data/config.json --strategy CcxtDryRunStrategy
# 想接 testnet 真测试单：把 config.json 里 "dry_run": true 改为 false
```

- `user_data/config.json`：交易所(testnet)、品种、仓位、dry_run 开关
- `user_data/strategies/CcxtDryRunStrategy.py`：EMA 金叉 + RSI 信号（与 `bot_dryrun` 同逻辑）
- 详见 `crypto/freqtrade/README.md`

---

## 七、钉钉推送（与 A股共用）

在 `.env` 配好 `DINGTALK_WEBHOOK` + `DINGTALK_SECRET`，并把 `CRYPTO_PUSH=true`，`bot_dryrun.py` 每轮信号会自动推到你的钉钉群（走 `a_share/notify.py` 的加签逻辑）。

> 未配置钉钉凭据时，`CRYPTO_PUSH=true` 也只会打印 `[warn] 推送失败` 而不报错，不影响主流程。

---

## 八、推荐日常用法

| 你的目的 | 该跑什么 |
|---------|---------|
| 第一次验证环境通不通 | `ccxt_demo.py` |
| 日常看信号、验证链路（最常用） | `bot_dryrun.py --once` 或 `--loop --interval 60` |
| 想看真实行情但不下单 | `bot_dryrun.py --once --live`（dry_run 保持 true） |
| 技术验证下测试单 | `--live` + `CRYPTO_DRYRUN=false` + 配 testnet key |
| 做回测/参数优化 | Freqtrade 框架版 |
| 每轮自动推钉钉 | `.env` 开 `CRYPTO_PUSH=true` |

---

## 九、合规与风险红线

- 🔴 **绝不接真实资金账户**（mainnet API key 不要填进 `.env`）。
- 🔴 境内访问境外交易所需合规自担，本模块仅技术验证用途。
- 🟢 默认 `dry_run=true` + 无密钥自动转离线合成，开箱即零风险。
- 所有密钥只在 `.env`，已被 `.gitignore` 忽略，勿提交。

---

## 十、与 A股模块的关系 & 下一步

- 加密模块与 A股模块**目前各自独立**：A股走 `a_share/`（四维度信号 + 可视面板），加密走 `crypto/`（CCXT 链路）。共用 `risk/risk_control.py` 风控与 `a_share/notify.py` 钉钉推送。
- **主页面加密行情面板（已实现，内联非弹窗）**：顶栏「🪙 加密行情」按钮切换显示，**直接内联在主页面**（不再弹窗）；加密行情条任意币种可点击直接进入该币详情。
  - 自选编辑：面板内「✎ 编辑自选」可增删币种（持久化到 `a_share/crypto_watchlist.json`，已 gitignore），行情条与下拉框实时同步；单币种取数失败仅跳过该币、不拖累整条。
  - 自动刷新：面板内置「自动刷新」勾选（默认开，每 30s 拉一次详情）；行情条仍每 10s 刷新。
  - 数据源：`datasource.py` 的 `fetch_crypto_kline`（任意周期 OHLCV，直连 Binance 公开域 `data-api.binance.vision`，无需密钥）+ `fetch_crypto_ticker24`（24h 高/低/额）+ `fetch_crypto_quotes`（读自选列表）。
  - 展示：KPI 卡（现价/24h涨跌/24h高/低/额/RSI/EMA状态/波动率/斜率）+ ECharts 蜡烛图（含成交量副图，**图上蓝色阴影带=下一周期 1σ 统计区间**）+ 短期研判面板。
  - 研判：`compute_crypto_indicators`（RSI14、EMA7/25 金叉死叉、ATR、近24根波动率、趋势斜率）+ `crypto_forecast`（基于波动率的下一周期 1σ 统计区间 + EMA/RSI 方向研判 + 买卖信号，复用 bot 的 EMA+RSI 规则）。**纯统计/技术指标研判，非投资建议**。
  - 接口：`/api/crypto_detail?symbol=BTC/USDT&timeframe=1h&limit=200`；自选管理 `GET/POST /api/crypto_watchlist`（`action=add|remove`，`symbol=XXX/USDT`）。
