# 套利模拟器使用手册（Polymarket 单源 · 虚拟盘）

> 适用模块：`a_share/arb_book.py`、`a_share/arbitrage.py`、`a_share/polymarket.py`、`a_share/arb_backtest.py`、看板 `模拟套利` 面板
> 版本：v4（手续费率可调 + 未实现盈亏重估 + 历史回测 + 权益曲线 + 成交时间轴）
> 最后更新：2026-08-22

---

## 1. 工具概述

本工具是一套**事件概率套利模拟器**，完全基于公开行情与虚拟本金，**不连接任何真实资金、不调用任何下单接口**。它把 Polymarket（链上预测市场）当作「事件概率」另类数据源，在只读盘口上模拟两类策略：

| 策略 | 一句话说明 | 风险特征 |
|------|-----------|---------|
| 🔁 单边做市价差 | 在高流动性市场买 bid / 卖 ask，库存中性下每轮锁定 spread | 低（成对锁利，库存受偏斜上限约束） |
| 🎯 同事件互斥套利 | 同事件多个互斥结果概率和偏离 1 时提示（**实验性，需人工确认完备性**） | 高（Poly 已消除大部分无风险免费钱，仅作人工复核线索） |

**为什么是 Polymarket 单源？** 原计划做「跨平台 Kalshi↔Poly 套利」，但 Kalshi 要求美国身份 + 美国 IP（CFTC 合规），本环境无法合规获取实时行情，故转为 Polymarket 单源深化。

### ⚠️ 合规红线（不可逾越）
- 全程过滤 **政治 / 地缘 / 战争 / 敏感人物** 类市场（`polymarket.py` 服务端强制过滤，前端无法绕过）。
- 只跑 **crypto / economy / finance / business / tech / science / sports / entertainment** 等中性类别。
- 仅读公开盘口、虚拟本金模拟成交。**任何情况下都不碰真实资金。**

---

## 2. 核心概念

| 概念 | 含义 |
|------|------|
| 💵 虚拟本金 | 默认 `$10,000`，仅用于模拟成交记账，不影响真实资产 |
| 📦 库存 / 净库存 | 某市场（market_id）的净持仓份额，`+` 为多、`-` 为空；`0` 表示中性 |
| 📐 偏斜上限 `max_skew` | **单市场最大净库存**（份额）。防止过度集中于单一市场。默认 `300`，**面板可调、持久化** |
| 💸 手续费率 `fee_rate` | **单边撮合手续费**（按成交额计）。默认 `1%`，**面板可调、持久化**。买/卖两腿各扣一次，锁定价差利润时一并扣除 |
| ↔️ 价差 spread | `ask − bid`，做市商可锁定的毛利空间 |
| 🦵 做市腿 mm_leg | 一次单边成交（买建仓 / 卖对冲） |
| 🔒 对冲平仓 | 反向成交使净库存归 0，锁定该轮 spread 利润 |
| 🧠 偏斜 skew% | `净库存 / max_skew`，用于可视化风险条（绿<50% <黄<80% <红≥80%） |

**做市模型（单边成交 + 自动反向对冲）：**
1. 库存为 0 → 首笔在 **bid 买入**建多仓（此时不锁利润，仅记库存）；
2. 库存 > 0（已净多）→ 本次在 **ask 卖出**对冲，库存归 0 时锁定 spread 利润；
3. 库存 < 0（已净空）→ 本次在 **bid 买入**对冲。
4. 单市场连续做市天然形成「建仓→对冲→建仓→对冲」循环，库存始终被 `max_skew` 约束。

---

## 3. 架构与文件

```
a_share/
  polymarket.py   只读行情层：拉 Gamma 公开 API，合规过滤，输出统一 Quote
  arbitrage.py    匹配层：scan_poly_marketmaking（做市机会+智能选股）、scan_poly_event_arb（事件套利）
  arb_book.py     虚拟账本：VirtualBook（现金/持仓/库存/偏斜上限/手续费率，持久化到 arb_book.json）
  arb_backtest.py 历史回测：run_backtest（拉 CLOB prices-history 真实 mid，构造双边盘口做成对做市，扣费）
  webui.py        看板服务：模拟套利面板 HTML + JS + /api/arb_* 接口
```

数据持久化文件：`a_share/arb_book.json`（与 A 股模拟盘 `sim_book.json` 完全隔离）。

---

## 4. 启动与访问

```bat
# 方式一：双击项目根目录的「启动看板.bat」（自动清旧进程 → 起 8787 双栈）
# 方式二：手动
cd E:\Workbuddy\Quant-Trading
python a_share/webui.py
```
启动后浏览器访问 **http://127.0.0.1:8787**。
在看板顶部导航点击 **🧪 模拟套利** 展开面板（默认收起，点标题栏「▴ 收起 / ▾ 展开」可折叠）。

> 注意：每次修改代码后需**重启服务**才能生效（杀掉 8787 端口进程再启动）。

---

## 5. 面板功能详解

### 5.1 ⚙️ 策略设置栏（新增）
位于面板顶部，集中管控风险与轮动行为：

| 控件 | 作用 |
|------|------|
| **偏斜上限** 输入框 + `设置` | 修改 `max_skew`（10~5000 整数），即时持久化到 `arb_book.json` |
| **手续费率%** 输入框 + `设置费率` | 修改 `fee_rate`（0~10%，即 0~0.1，按单边成交额计），即时持久化；越界被拒绝 |
| ☑ **轮动跳过高偏斜市场** | 自动轮动时启用「智能选股」（默认勾选） |
| ☑ **轮动后自动再平衡** | 轮动结束后对剩余偏斜市场强制对冲平仓（可选） |
| **最小流动性** | 仅对流动性 ≥ 此值的市场轮动（0 = 不限制） |
| **轮动份额** | 轮动每笔固定份额（0 = 用各机会默认 `size_hint`） |
| 🗑 **重置账本** | 清空本金/持仓/库存（**保留偏斜上限设置**），二次确认后执行 |

### 5.2 🔁 单边做市价差表
每行一个高流动性市场，列含义：

| 列 | 说明 |
|----|------|
| 市场 | 题目；若你已在该市场有持仓，追加显示 **本仓 +NN** 徽标；达偏斜上限则显示 ⚠ 已达上限（整行置灰） |
| 买 / 卖 | 当前 bid / ask（隐含概率） |
| 价差 | `ask−bid`，括号内为占中间价的百分比 |
| 每单位锁定 | 库存中性下每份额可锁毛利 `spread/2` |
| 流动性 | 市场流动性（美元） |
| 份额 | 本次模拟成交份额（默认可改） |
| 模拟做市 | 单点一次单边成交（买建仓 / 卖对冲，由当前库存自动决定方向） |

### 5.3 🎯 同事件互斥套利（实验性）
同事件下多个互斥结果（win/draw/away 等）概率和偏离 1 时提示，但 **Polymarket 已消除大部分无风险免费钱**，一律标 ⚠️ 需人工确认是否互斥完备，**不提供一键下单**。

### 5.4 📊 库存偏斜控制
展示每个有库存的市场：净库存、偏斜百分比（颜色风险条）、`⚖️ 再平衡·对冲平仓` 按钮（按实时价强制对冲，库存归 0，锁定对应价差利润）。

### 5.5 📒 持仓 / 盈亏（虚拟）

顶部摘要栏实时展示：

| 指标 | 含义 |
|------|------|
| 虚拟本金 | 初始注入的总资金（默认 `$10,000`） |
| 可用现金 | `cash`，扣费后的即时可用余额 |
| 已实现盈亏 | 库存归 0 时锁定的价差利润（已扣双边手续费） |
| **未实现盈亏** | 对每个仍有净库存的市场，按实时 mid 重估：`(mid − 均价) × 净库存`（净空取反向），逐市场累加 |
| **权益市值** | `equity = 现金 + 未实现盈亏`，反映当前虚拟账户真实价值 |
| **费率** | 当前生效的 `fee_rate`（%），所有锁利计算均按此扣费 |
| 未平仓腿 | 尚未对冲归零的做市腿数量 |

> 未实现盈亏采用**逐市场按 mid 盯市**重估（实时价取自 `polymarket.fetch_poly_quotes`），库存中性（净库存 0）的市场不贡献未实现盈亏。

**📈 账户资金曲线**：持仓列表上方自动绘制 SVG 折线（蓝线 `#5fa8d3`），横轴为成交顺序、纵轴为每笔成交后的 `cash_after`，并以 `$10000` 为基准虚线。一眼看清本金随逐笔成交的增减轨迹。

**🕒 成交时间轴**：逐条持仓按时间倒序列出——`标签（建多仓/对冲卖/买/卖）@价格 ×份额 费$ 时间戳 余$现金`，点 `结算` 可单条演示性平仓。`时间戳` 为真实成交时刻（`new Date(p.ts*1000)`），方便复盘每笔动作。

### 5.6 📈 历史回测（验证策略历史表现）

面板底部「历史回测」区，对**真实历史价格**跑做市策略回测，只读公开数据、不涉真实资金。

**回测模型**（`arb_backtest.run_backtest`）：
1. 拉取该市场 CLOB `prices-history` 的真实 mid 序列（按 `days` 截断、按 `every_min` 重采样）；
2. 以固定**半价差** `half_spread` 构造双边盘口：`bid = p*(1−half)`，`ask = p*(1+half)`（`half_spread` 默认取当前实时价差一半）；
3. 相邻两点做成对做市：`上一期@bid 建多 → 本期@ask 对冲`，每对扣单边手续费 `fee = (enter_bid + exit_ask) × size × fee_rate`；
4. 累加权益、统计胜率/净盈亏/最大回撤，输出权益曲线。

> **刻意保留两个真实约束**（防止结论被美化）：① 价差必须大于手续费才有油水——窄价差市场扣费后必亏；② 持仓期价格方向性风险——价格反向则该笔亏损。

**面板参数**：

| 控件 | 说明 | 默认 |
|------|------|------|
| 市场 | 下拉框，自动载入当前扫描Top20市场 | — |
| 回看天数 | 历史窗口长度（天） | 30 |
| 频率(分钟) | 每隔多少分钟做一次成对做市 | 1440（每日一笔） |
| 份额 | 每笔成交份额 | 100 |

点 `📈 运行回测` → 结果区显示：样本点 / 交易笔数 / 胜率 / 净盈亏 / 最终权益 / 最大回撤 / 半价差 / 费率，并绘制**回测权益曲线**（绿线，基准 `$10000` 虚线）。

---

## 6. 操作流程

### A. 手动做市（单市场精细操作）
1. 在「单边做市价差表」找到目标市场，修改「份额」；
2. 点 `模拟做市`：库存 0 时建多仓，已有库存时自动反向对冲；
3. 看 📊 库存偏斜控制 与 📒 持仓/盈亏 实时变化。

### B. 自动轮动做市（一键批量）
1. 在策略设置栏确认 **轮动跳过高偏斜市场** 已勾选；
2. 填「轮动笔数 N」（面板顶部输入框，默认 5）与可选「最小流动性 / 轮动份额」；
3. 点 `🔄 自动轮动做市`：
   - 取前 N 个最优做市机会；
   - 每个市场先买建仓、立刻卖对冲，成对锁定 spread；
   - **智能选股**自动跳过已偏斜 / 余量不足的市场；
   - 若勾选「轮动后自动再平衡」，结束再清一次残留偏斜。

### C. 再平衡（释放偏斜）
点 `⚖️ 再平衡·对冲平仓`，按实时价把仍偏斜的市场强制对冲至库存 0。

### D. 重置账本
点 `🗑 重置账本` → 确认 → 本金/持仓/库存清空（**偏斜上限保留**）。

### E. 历史回测（验证策略）
1. 先点 `刷新扫描` 载入市场列表（回测「市场」下拉依赖此列表）；
2. 在「历史回测」区选市场、设回看天数/频率/份额；
3. 点 `📈 运行回测`：拉取真实历史 mid，构造双边盘口做成对做市，扣费后输出权益曲线与统计。
> 回测**使用当前账本设置的 `fee_rate`**；净盈亏为虚拟，仅用于验证策略在不同历史区间的表现，不构成任何投资建议。

---

## 7. 智能选股（核心新增）

「轮动时跳过高偏斜市场」由 **两层防护** 实现，确保轮动不会越做越偏：

**① 扫描层排除**（`arbitrage.scan_poly_marketmaking`）
轮动扫描时，若某市场当前净库存 `|净库存| ≥ 0.8 × max_skew`，直接不进入候选名单。
> 例：`max_skew=100`，某市场已持仓 90（skew 0.9）→ 被排除。

**② 轮动余量检查**（`/api/arb_auto_mm` 服务端）
即便进入候选，若「本轮份额会让 |净库存| 触顶」即 `max_skew − |净库存| < 份额`，则跳过该市场。
> 例：`max_skew=100`，某市场已持仓 50（余量 50），轮动份额 100 → `50 < 100` → 跳过。

两层叠加，既避免浪费轮动名额，也避免库存失控。关闭「轮动跳过高偏斜市场」开关即停用两层防护（仅手动场景需要）。

---

## 8. API 参考

所有接口返回 JSON，`Content-Type: application/json; charset=utf-8`。

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/arb_opps` | GET | 做市机会 + 事件套利；返回 `marketmaking / event_arb / poly_count / max_skew / inventory`（含各市场当前净库存，供前端「本仓」标注） |
| `/api/arb_book` | GET | 虚拟账本快照（**注入实时价映射做盯市重估**）：返回 `cash / bankroll / realized_pnl / open_positions / max_skew / fee_rate / unrealized_pnl / equity / inventory(含 mid,unrealized) / positions(含 fee,ts,cash_after)` |
| `/api/arb_mm` | POST | 手动做市 `{opp, size}` → `market_make()` |
| `/api/arb_auto_mm` | GET | 自动轮动，参数：<br>`n`（笔数 1~50，默认 5）<br>`skip`（1/0 智能选股，默认 1）<br>`reb`（1/0 轮后自动再平衡，默认 0）<br>`minliq`（最小流动性，默认 0）<br>`size`（轮动份额，0=默认） |
| `/api/arb_set_skew` | POST | 设置偏斜上限 `{value}`（10~5000，越界返回 `ok:false`） |
| `/api/arb_set_fee` | POST | 设置手续费率 `{value}`（0~0.1，即 0%~10%，越界返回 `ok:false`；按单边成交额计） |
| `/api/arb_backtest` | GET | 历史回测，参数：`market`（市场ID）<br>`days`（回看天数，默认30）<br>`every`（频率分钟，默认1440）<br>`size`（每笔份额，默认100）；使用当前账本 `fee_rate`；返回统计 + `curve` 权益曲线 |
| `/api/arb_rebalance` | POST | 按实时价强制对冲所有偏斜市场 |
| `/api/arb_reset` | POST | 重置账本（保留偏斜上限） |
| `/api/arb_demo` | GET | 载入跨平台演示对（示例价，非实时） |
| `/api/arb_execute` | POST | 执行演示对套利 `{opp, size}` |
| `/api/arb_settle` | POST | 结算单条持仓 `{pid}` |

`/api/arb_auto_mm` 返回示例：
```json
{
  "ok": true,
  "executed": 4,
  "skipped": 1,
  "locked_total": 39.80,
  "msg": "对冲平仓：卖 @0.4600 ×100，净库存归 0，锁定价差收益 $12.00 …"
}
```

---

## 9. 数据文件 `arb_book.json`

```json
{
  "cash": 10000.0,
  "bankroll": 10000.0,
  "realized_pnl": 0.0,
  "positions": [ { "pid":"MM1", "kind":"mm_leg", "side":"buy", "mkt":"1383905", "entry":0.34, "size":100, "ts":... } ],
  "inventory": { "1383905": 0 },
  "avg_cost": { "1383905": 0.0 },
  "inv_q": { "1383905": "市场题目" },
  "seq": 1,
  "max_skew": 300,
  "fee_rate": 0.01
}
```
> 修改 `max_skew` / `fee_rate` 后无需改代码，重启服务会自动读取此处值。

---

## 10. 参数与风控速查

| 参数 | 默认值 | 范围 | 说明 |
|------|--------|------|------|
| `max_skew` | 300 | 10~5000 | 单市场最大净库存；越小越保守 |
| `fee_rate` | 0.01（1%） | 0~0.1 | 单边撮合手续费率；买/卖两腿各扣一次，锁利时一并扣除；回测也沿用此值 |
| 做市最小价差 | 0.003 | — | 低于此价差的市场不列为机会 |
| 轮动笔数 N | 5 | 1~50 | 单次轮动最大成交数 |
| 最小流动性 | 0 | ≥0 | 轮动过滤门槛 |
| 事件套利最小利润 | 0.004 | — | 低于此不列为线索 |

**风控建议**：实盘模拟学习期建议把 `max_skew` 设在 100~300；`最小流动性` 设 1000 以上避免薄价市场滑点；`轮动跳过高偏斜市场` 保持勾选。

---

## 11. 常见问题 FAQ

**Q：轮动后某市场仍显示偏斜？**
A：轮动成对锁利后库存归 0；若手动开了仓未对冲，点 `⚖️ 再平衡` 或勾选「轮动后自动再平衡」。

**Q：偏斜上限设置不生效？**
A：需点 `设置` 按钮提交；值须在 10~5000，越界会被拒绝。设置写入 `arb_book.json`，重启仍保留。

**Q：为什么事件套利没有一键下单？**
A：Polymarket 已消除大部分无风险免费钱，同事件结果是否「互斥且完备」需人工判断，故仅提供线索，不自动成交。

**Q：扫描很慢 / 返回 0 条？**
A：Gamma API 偶发限流或网络波动，点 `刷新扫描` 重试；超过缓存 TTL（60s）会重新翻页拉取。

**Q：Kalshi 能用吗？**
A：本环境 Kalshi 公开行情因美国身份/IP 限制不可用（返回 0），故整套转向 Polymarket 单源。

**Q：手续费率设置不生效？**
A：需点 `设置费率` 按钮提交；值须在 0~10%（即 0~0.1）内，越界会被拒绝。设置写入 `arb_book.json`，重启仍保留。扣费在买/卖两腿各计一次，锁定价差利润时会一并扣除。

**Q：回测净盈亏是负的 / 很低？**
A：这是**真实约束**的体现——Polymarket 高流动性市场价差极窄（常 <1%），扣掉 1% 手续费后成对做市往往无利可图；只有价差显著大于费率的时段/市场才会赚钱。回测结果用于检验策略，不构成投资建议。

**Q：未实现盈亏和权益市值怎么算？**
A：对每个仍有净库存的市场，按实时 mid 盯市：`未实现 = (mid − 建仓均价) × 净库存`（净空取反向）；`权益市值 = 现金 + 全部未实现盈亏`。库存归 0 的市场不贡献未实现盈亏。

---

## 12. 风险提示与免责声明

- 本工具为**教学 / 策略模拟**用途，所有成交均为虚拟本金记账，**不涉及任何真实资金与交易**。
- 预测市场（Polymarket）价格随真实事件波动，模拟结果**不构成任何投资建议**。
- 使用者须自行遵守所在国家/地区法律法规，尤其不得用于政治、地缘等敏感市场。
- 作者不对模拟结果用于实盘决策导致的任何损失负责。

---
*本手册随代码同步维护；功能变动请以 `git log` 与 `arb_book.py` / `arbitrage.py` 源码注释为准。*
