# 备忘录 / TODO

## 定时任务触发时间调整：15:35 → 16:00
- 任务名：`QuantTrading_DailyReport`（Windows 任务计划程序）
- 当前触发：工作日（周一至周五）15:35
- 目标触发：16:00（等盘后数据更全，避免盘中数据未刷新导致信号偏差）
- 调整方案（二选一）：
  1. **手动**：`Win+R` → 输入 `taskschd.msc` → 任务计划程序库 → `QuantTrading_DailyReport` → 属性 → 触发器 → 编辑 → 开始时间改为 `16:00` → 确定
  2. **命令**（需 WorkBuddy 安全中心允许「系统级工具」策略后可用）：
     ```bat
     schtasks /change /tn "QuantTrading_DailyReport" /st 16:00
     ```
- 备注：本机曾因 WorkBuddy 安全策略禁用 `schtasks` 而无法用命令调整，故先用方案 1 手动改；策略开放后可用方案 2。
- 入口脚本：`run_daily_report.bat`（已随仓库提交），调用 `run_daily.py --screener` 生成收盘日报（四维度信号 + 五板块选股）并推送钉钉。
